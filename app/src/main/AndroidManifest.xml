<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required to draw the overlay near the top status/clock bar. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Foreground service so the overlay keeps running during a ride. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_extension_logo"
        android:label="@string/app_name"
        android:theme="@style/Theme.KarooPhoneStatus"
        tools:targetApi="33">

        <!-- Launcher activity: appears in the Karoo app menu, handles the
             one-time permissions, then starts the service. -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Overlay foreground service. -->
        <service
            android:name=".PhoneStatusOverlayService"
            android:exported="false" />

        <!-- Watches the systemui status bar so the indicator mirrors it
             (visibility + position). Read-only; takes no actions. -->
        <service
            android:name=".StatusBarMonitorService"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            tools:ignore="AccessibilityPolicy">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Karoo extension registration: the Karoo system binds this service
             at boot, which auto-starts the overlay service. -->
        <service
            android:name=".PhoneStatusExtension"
            android:exported="true"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="io.hammerhead.karooext.KAROO_EXTENSION" />
            </intent-filter>
            <meta-data
                android:name="io.hammerhead.karooext.EXTENSION_INFO"
                android:resource="@xml/extension_info" />
        </service>

    </application>
</manifest>
