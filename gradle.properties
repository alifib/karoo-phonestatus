org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
android.useAndroidX=true
kotlin.code.style=official

# --- karoo-ext GitHub Packages credentials ---------------------------------
# GitHub Packages requires auth even for public packages. Do NOT commit real
# values here. Put them in your *global* ~/.gradle/gradle.properties instead,
# or export USERNAME / TOKEN env vars (used as a fallback in settings.gradle.kts).
#
# gpr.user=your-github-username
# gpr.token=ghp_your_personal_access_token_with_read:packages_scope
